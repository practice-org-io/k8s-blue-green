apiVersion: v1 
kind: Deployment
metadata:
  name: nginx-blue
  # these are replicaset labels.
  labels:
    project: roboshop
    purpose: rs-demo 
    tier: frontend
    component: frontend
    version: blue
spec:
  replicas: 3
  selector:
    # Using these labels replicaset selects the pod.
    matchLabels:
      project: roboshop
      purpose: rs-demo 
      tier: frontend
      component: frontend
      version: blue
  # This is pod definition,  pod is subset of replicaset
  template:
  metadata: 
    labels:
      project: roboshop
      purpose: rs-demo 
      tier: frontend
      component: frontend
      version: blue
    spec:
      containers:
      - name: nginx-blue
        image: lokeshlh/blue-green:0.0.1